<!DOCTYPE html>
<html lang="pt-br">
<head>
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@48,400,0,0" />
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Teste Pratico NUTI</title>
    <style>
        body {
            background-color: aquamarine;
        }
        #conteudo {
            margin: 20px auto; 
            max-width: 800px; 
            padding: 20px;
            border: 2px solid black; 
            background-color: white;
        }
        h1 {
            text-align: center;
            font-family: Arial, sans-serif;
        }
        form {
            margin: 0;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        table {
            border-collapse: collapse;
            width: 400px; 
            font-family: Arial, sans-serif;
            margin: 0 auto;
            margin-top: 20px; 
        }

        th, td {
            border: 1px solid #000000;
            text-align: center;
            padding: 8px;
        }

        th {
            background-color: #ffffff;
            font-weight: bold;
        }

        tr:nth-child(even) {
            background-color: #ffffff;
        }

        tr:hover {
            background-color: #ddd;
        }
        button {
            padding: 3px;
            bottom: 10px;
            margin-left: 1.5px;
            border-radius: 5px;
        }
        .material-symbols-outlined {
        font-size: 17px;
        }

        .texto {
            padding: 5px;
        }

    </style>
</head>
<body>
    <div id="conteudo">
    <h1>Quantidade de tags HTML</h1>
    <form action="/executar" method="post">
        <input type="text" name="url" placeholder="Insira a URL" class="texto">
        <button type="submit" class="material-symbols-outlined">
            <i class="material-icons">search</i>
        </button>
    </form>
    <form action="/acabar" method="post">
        <input type="submit" value="Fechar Conexão">
    </form>
    <% if (resultados && resultados.length > 0) { %>
        <table>
            <thead>
                <tr>
                    <th>Tag</th>
                    <th>Quantidade</th>
                </tr>
            </thead>
            <tbody>
                <% resultados.forEach(resultado => { %>
                    <tr>
                        <td><%= resultado.tag %></td>
                        <td><%= resultado.quantidade %></td>
                    </tr>
                <% }); %>   
            </tbody>
        </table>
    <% } %>
    </div>
</body>
</html>
